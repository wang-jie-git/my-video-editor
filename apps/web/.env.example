# Environment variables example
# Copy this file to .env.local and update the values as needed

# Node
NODE_ENV=development

# Public
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ========================================
# One Memory MCP Configuration (Optional)
# ========================================
# Enable One Memory MCP integration for AI assistant memory capabilities
# Only works in Node.js environment (Server Components, API Routes, Server Actions)
# NEXT_PUBLIC_MCP_ENABLED=true

# Path to One Memory MCP Server executable
# NEXT_PUBLIC_MCP_SERVER_PATH=/Users/mac/Desktop/AI-memory/packages/memory-mcp/build/index.js

# CodeGraph directory path (optional, auto-detected if not provided)
# NEXT_PUBLIC_CODEGRAPH_DIR=/path/to/your/project/.codegraph

# Embedder type: simple (default, zero setup) | api | local
# NEXT_PUBLIC_MCP_EMBEDDER=simple

# Embedder API configuration (only needed for embedder=api)
# NEXT_PUBLIC_MCP_EMBEDDER_API_KEY=your_api_key
# NEXT_PUBLIC_MCP_EMBEDDER_BASE_URL=https://api.example.com/v1
# NEXT_PUBLIC_MCP_EMBEDDER_MODEL=text-embedding-3-small

# Server
DATABASE_URL="postgresql://cutia:cutia@localhost:5432/cutia"
BETTER_AUTH_SECRET=your_better_auth_secret

UPSTASH_REDIS_REST_URL=http://localhost:8079
UPSTASH_REDIS_REST_TOKEN=example_token_here

FREESOUND_CLIENT_ID=your_client_id_here
FREESOUND_API_KEY=your_api_key_here

# Cloudflare R2 (for reference image uploads)
R2_ACCOUNT_ID=your_r2_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=your_r2_bucket_name
R2_PUBLIC_URL=https://your-r2-public-url.example.com